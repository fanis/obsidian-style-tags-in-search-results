# vscode
.vscode 

# Intellij
*.iml
.idea

# npm
node_modules


# Built plugin bundle - produced by `npm run build`, attached to GitHub releases
main.js

# Exclude sourcemaps
*.map

# Exclude macOS Finder (System Explorer) View States
.DS_Store

# Plugin runtime data — must never be bundled into releases
data.json

# Test runner output
coverage/

# Claude Code local config (commands, permissions, etc.)
.claude/
.claude.local.md

# AI-agent kickstart notes — local only
AGENTS.md
